extends layout

block content
    .wrapper
        include sidebar
        #content
            //h1 #{title}
            ul.list-group
                // each article, i in articles
                //     li.list-group-item= article.title
    include nav_bar

    script(type='text/javascript').
        $(document).ready(function () {
            localStorage.setItem("selectedTab", "your_health");
        });